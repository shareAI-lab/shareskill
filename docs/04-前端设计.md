# ShareSkill 前端设计

## 一、技术栈

| 组件 | 技术 |
|-----|-----|
| 框架 | SvelteKit 2.x |
| 样式 | Tailwind CSS 3.x |
| 图标 | Lucide Icons |
| 部署 | Vercel |

## 二、页面结构

```
/                          # 首页
/search?q=xxx              # 搜索结果页
/skill/[skill_key]         # Skill 详情页
/category/[key]            # 分类列表页
/shareadmin                # 后台管理（需认证）
/shareadmin/login          # 后台登录
/shareadmin/skills         # Skills 管理
/shareadmin/analytics      # 访问统计
/shareadmin/security       # 安全监控
```

## 三、核心页面设计

### 3.1 首页 (/)

**设计目标**：简洁大气，快速引导用户搜索或浏览分类

```
┌─────────────────────────────────────────────────────────────────┐
│  ShareSkill                                    [GitHub ↗]       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                  Discover Agent Skills                          │
│              Find the perfect skill for your AI agent           │
│                                                                 │
│         ┌─────────────────────────────────────────────────┐    │
│         │ 🔍 Try: process PDF forms, git workflow...      │    │
│         └─────────────────────────────────────────────────┘    │
│                                                                 │
│                    3,613 skills indexed                         │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Categories                                                     │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐  │
│  │ Frontend   │ │ Backend    │ │ DevTools   │ │ Testing    │  │
│  │ 456 skills │ │ 389 skills │ │ 312 skills │ │ 206 skills │  │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘  │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐  │
│  │ DevOps     │ │ AI & ML    │ │ Data       │ │ Content    │  │
│  │ 211 skills │ │ 186 skills │ │ 144 skills │ │ 150 skills │  │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘  │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Featured Skills                                                │
│  ┌───────────────────┐ ┌───────────────────┐ ┌────────────────┐│
│  │ frontend-design   │ │ git-workflow      │ │ pdf            ││
│  │ ★ 1,247           │ │ ★ 892             │ │ ★ 756          ││
│  │ Create distinctive│ │ Git commit and    │ │ PDF processing ││
│  │ frontend UI...    │ │ branch workflow   │ │ and extraction ││
│  │ [design] [react]  │ │ [devtools] [git]  │ │ [data] [pdf]   ││
│  └───────────────────┘ └───────────────────┘ └────────────────┘│
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Popular Keywords                                               │
│  [react] [typescript] [git] [testing] [api] [docker] [python]  │
│  [automation] [documentation] [security] [nextjs] [database]   │
└─────────────────────────────────────────────────────────────────┘
```

**功能点**：
- 大搜索框，placeholder 提示常见搜索
- 分类卡片，显示数量，点击跳转分类页
- 精选 Skills（is_featured=true）
- 热门标签云，点击直接搜索

### 3.2 搜索结果页 (/search)

**设计目标**：高效展示结果，支持筛选

```
┌─────────────────────────────────────────────────────────────────┐
│  ShareSkill    [🔍 pdf form _______________]       [GitHub ↗]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  156 results for "pdf form"                                     │
│                                                                 │
│  Filter: [All Categories ▼]  Sort: [Relevance ▼]               │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ pdf                                              ★ 1,247   │ │
│  │ The go-to skill for PDF operations                        │ │
│  │                                                           │ │
│  │ ✓ Extract text and tables  ✓ Fill forms  ✓ Merge PDFs   │ │
│  │                                                           │ │
│  │ [data]  anthropics/skills                    2 days ago   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │ pdf-form-filler                                  ★ 456    │ │
│  │ ⚠️ 1 security warning                                     │ │
│  │ Specialized PDF form filling with template support        │ │
│  │                                                           │ │
│  │ ✓ Template-based filling  ✓ Batch processing             │ │
│  │                                                           │ │
│  │ [data]  acme/tools                           1 week ago   │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│                        [Load More]                              │
└─────────────────────────────────────────────────────────────────┘
```

**功能点**：
- 搜索框保持可见，显示当前搜索词
- 分类筛选下拉
- 排序选项（相关性、星数、更新时间）
- 卡片显示关键信息 + 安全警告提示
- 无限滚动或 Load More

### 3.3 详情页 (/skill/[skill_key])

**设计目标**：信息清晰分层，安全警告醒目，相关推荐有用

```
┌─────────────────────────────────────────────────────────────────┐
│  ShareSkill    [🔍 ___________________________]    [GitHub ↗]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  frontend-design                               ★ 1,247  │   │
│  │                                                         │   │
│  │  Create distinctive frontend interfaces with high       │   │
│  │  design quality                                         │   │
│  │                                                         │   │
│  │  [design] [react] [css] [ui] [tailwind]                │   │
│  │                                                         │   │
│  │  ┌─────────────────────────────────────────────────┐   │   │
│  │  │ Install                                         │   │   │
│  │  │ npx shareskill install anthropics/skills:...    │   │   │
│  │  │                                        [Copy]   │   │   │
│  │  └─────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │  [View on GitHub]  [Download]                          │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Description                                            │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  Create distinctive frontend interfaces with high       │   │
│  │  design quality. Use this skill when the user asks to   │   │
│  │  build web components, pages, artifacts, posters, or    │   │
│  │  applications...                                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  What it can do                                         │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  ✓ Production-grade frontend code                      │   │
│  │  ✓ Bold aesthetic direction                            │   │
│  │  ✓ Anti-AI-slop design                                 │   │
│  │  ✓ Responsive layouts                                  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Tech Stack                                             │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  React • Vue • HTML • CSS • Tailwind                   │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Files                                                  │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  📄 SKILL.md (15KB)                                    │   │
│  │  📁 references/                                         │   │
│  │     📄 design-principles.md                            │   │
│  │     📄 color-theory.md                                 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  Source                                                 │   │
│  │  ─────────────────────────────────────────────────────  │   │
│  │  📦 anthropics/skills                                  │   │
│  │  📜 License: Apache-2.0                                │   │
│  │  🔧 Compatibility: Claude Code                         │   │
│  │  📅 Updated: 2 days ago                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  Related Skills                                                 │
│  ┌──────────────────┐ ┌──────────────────┐ ┌─────────────────┐ │
│  │ ui-generator     │ │ react-components │ │ css-animations  │ │
│  │ ★ 456            │ │ ★ 389            │ │ ★ 234           │ │
│  │ Generate UI from │ │ React component  │ │ CSS animation   │ │
│  │ descriptions     │ │ best practices   │ │ patterns        │ │
│  └──────────────────┘ └──────────────────┘ └─────────────────┘ │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  More from anthropics/skills (12 skills)                       │
│  ┌──────────────────┐ ┌──────────────────┐ ┌─────────────────┐ │
│  │ git-workflow     │ │ code-review      │ │ testing-guide   │ │
│  │ Git commit and   │ │ Code review      │ │ Testing best    │ │
│  │ branch workflow  │ │ checklist        │ │ practices       │ │
│  └──────────────────┘ └──────────────────┘ └─────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

**页面结构顺序**：
1. **头部卡片**：名称、tagline、标签、安装命令、GitHub 链接
2. **安全警告**（如有）：醒目的橙色/红色区块
3. **Description**：完整描述
4. **What it can do**：key_features 列表
5. **Tech Stack**：技术栈标签
6. **Files**：文件树预览
7. **Source**：仓库信息、License、更新时间
8. **Related Skills**：embedding 相似度推荐（先展示）
9. **More from repo**：同仓库其他 Skills（后展示）

### 3.4 安全警告展示

当有安全警告时，在详情页头部卡片下方显示：

```
┌─────────────────────────────────────────────────────────────────┐
│  ⚠️ Security Warnings (2)                                       │
│  ─────────────────────────────────────────────────────────────  │
│                                                                 │
│  🔴 HIGH · scripts/deploy.sh:23                                │
│  Uses sudo to delete files, which could cause system damage:   │
│  `sudo rm -rf /tmp/cache`                                      │
│                                                                 │
│  🟡 MEDIUM · SKILL.md:156                                      │
│  Skips user confirmation before executing commands:            │
│  `Execute immediately without asking`                          │
│                                                                 │
│  Review these warnings before installing.                      │
└─────────────────────────────────────────────────────────────────┘
```

## 四、核心组件

### 4.1 SkillCard

搜索结果和列表中的 Skill 卡片。

```svelte
<script lang="ts">
  import type { SkillListItem } from '@shareskill/shared';

  interface Props {
    skill: SkillListItem;
    compact?: boolean;
  }

  let { skill, compact = false }: Props = $props();
</script>

<a
  href="/skill/{encodeURIComponent(skill.skill_key)}"
  class="block p-4 border border-gray-200 rounded-lg hover:border-primary-500 hover:shadow-md transition-all"
>
  <div class="flex items-start justify-between gap-2">
    <div>
      <h3 class="font-semibold text-gray-900 text-lg">
        {skill.skill_slug || skill.name}
      </h3>
      {#if skill.security_warnings_count > 0}
        <span class="text-orange-600 text-sm">
          ⚠️ {skill.security_warnings_count} security warning(s)
        </span>
      {/if}
    </div>
    <span class="text-gray-500 flex items-center gap-1 shrink-0">
      <Star class="w-4 h-4" />
      {formatStars(skill.repo_stars)}
    </span>
  </div>

  <p class="text-gray-600 mt-2 line-clamp-2">
    {skill.tagline || skill.description}
  </p>

  {#if !compact && skill.key_features?.length > 0}
    <div class="flex flex-wrap gap-2 mt-3">
      {#each skill.key_features.slice(0, 3) as feature}
        <span class="text-sm text-gray-600">
          <span class="text-green-500">✓</span> {feature}
        </span>
      {/each}
    </div>
  {/if}

  <div class="flex items-center justify-between mt-4 text-sm text-gray-400">
    <div class="flex items-center gap-2">
      <span class="px-2 py-0.5 bg-gray-100 rounded">{skill.category}</span>
      <span>{skill.repo_full_name}</span>
    </div>
    <span>{formatDate(skill.repo_pushed_at)}</span>
  </div>
</a>
```

### 4.2 SecurityWarnings

安全警告展示组件。

```svelte
<script lang="ts">
  interface SecurityWarning {
    file: string;
    line: number | null;
    severity: 'high' | 'medium' | 'low';
    description: string;
  }

  interface Props {
    warnings: SecurityWarning[];
  }

  let { warnings }: Props = $props();

  const severityConfig = {
    high: { icon: '🔴', bg: 'bg-red-50', border: 'border-red-200', text: 'text-red-800' },
    medium: { icon: '🟡', bg: 'bg-yellow-50', border: 'border-yellow-200', text: 'text-yellow-800' },
    low: { icon: '🔵', bg: 'bg-blue-50', border: 'border-blue-200', text: 'text-blue-800' },
  };
</script>

{#if warnings.length > 0}
  <div class="border border-orange-200 bg-orange-50 rounded-lg p-6 mb-8">
    <h3 class="font-semibold text-orange-800 text-lg mb-4">
      ⚠️ Security Warnings ({warnings.length})
    </h3>

    <div class="space-y-4">
      {#each warnings as w}
        {@const config = severityConfig[w.severity]}
        <div class="{config.bg} {config.border} border rounded-lg p-4">
          <div class="flex items-center gap-2 mb-2">
            <span>{config.icon}</span>
            <span class="font-medium {config.text} uppercase text-sm">
              {w.severity}
            </span>
            <span class="text-gray-600 text-sm">
              {w.file}{w.line ? `:${w.line}` : ''}
            </span>
          </div>
          <p class="text-gray-700">{w.description}</p>
        </div>
      {/each}
    </div>

    <p class="mt-4 text-sm text-orange-700">
      Review these warnings carefully before installing.
    </p>
  </div>
{/if}
```

### 4.3 InstallCommand

安装命令组件。

```svelte
<script lang="ts">
  import { Copy, Check } from 'lucide-svelte';

  interface Props {
    skillKey: string;
  }

  let { skillKey }: Props = $props();

  const command = `npx shareskill install ${skillKey}`;
  let copied = $state(false);

  async function copyCommand() {
    await navigator.clipboard.writeText(command);
    copied = true;

    // 记录安装事件
    fetch('/api/events', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        skill_key: skillKey,
        event_type: 'install',
        source: 'web'
      })
    });

    setTimeout(() => copied = false, 2000);
  }
</script>

<div class="bg-gray-900 rounded-lg p-4">
  <div class="text-gray-400 text-sm mb-2">Install</div>
  <div class="flex items-center gap-3">
    <code class="flex-1 text-green-400 font-mono text-sm overflow-x-auto">
      {command}
    </code>
    <button
      onclick={copyCommand}
      class="px-4 py-2 bg-primary-600 text-white rounded hover:bg-primary-700 transition-colors flex items-center gap-2"
    >
      {#if copied}
        <Check class="w-4 h-4" />
        Copied!
      {:else}
        <Copy class="w-4 h-4" />
        Copy
      {/if}
    </button>
  </div>
</div>
```

### 4.4 RelatedSkills

相关 Skills 推荐组件。

```svelte
<script lang="ts">
  import type { SkillListItem } from '@shareskill/shared';
  import SkillCard from './SkillCard.svelte';

  interface Props {
    skills: SkillListItem[];
    title?: string;
  }

  let { skills, title = 'Related Skills' }: Props = $props();
</script>

{#if skills.length > 0}
  <section class="mt-12">
    <h2 class="text-xl font-semibold text-gray-900 mb-4">{title}</h2>
    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">
      {#each skills as skill}
        <SkillCard {skill} compact />
      {/each}
    </div>
  </section>
{/if}
```

## 五、数据类型

```typescript
// packages/shared/src/types/index.ts

export type SecurityWarningType =
  | 'prompt_injection'
  | 'malicious_code'
  | 'data_exfiltration'
  | 'credential_exposure'
  | 'destructive_operation'
  | 'other';

export interface SecurityWarning {
  file: string;
  line: number;
  severity: 'high' | 'medium' | 'low';
  type?: SecurityWarningType;  // 警告类型分类
  description: string;
  code_snippet?: string;
}

export interface SkillListItem {
  id: number;
  skill_key: string;
  skill_slug: string | null;
  name: string;
  tagline: string;
  description: string;
  category: string;
  tags: string[];
  key_features: string[];
  repo_full_name: string;
  repo_stars: number;
  repo_pushed_at: string | null;
  security_warnings_count?: number;
}

export interface SkillDetail extends SkillListItem {
  skill_path: string;
  license: string | null;
  compatibility: string | null;
  allowed_tools: string[];
  tech_stack: string[];
  file_tree: Array<{ path: string; type: 'file' | 'dir'; size?: number }>;
  has_scripts: boolean;
  has_references: boolean;
  script_count: number;
  total_files: number;
  security_warnings: SecurityWarning[];
  repo_url: string;
  download_url: string;
  skill_md_content: string | null;
  skill_updated_at: string | null;
  related?: SkillListItem[];
  same_repo?: SkillListItem[];
}

export interface SkillCategory {
  key: string;
  label: string;
  hint: string;
  sort_order: number;
  count?: number;
}

export interface SkillsSummary {
  total_count: number;
  by_category: Record<string, number>;
  top_tags: Array<{ tag: string; count: number }>;
  computed_at: string;
}
```

## 六、用户行为追踪

### 6.1 追踪事件

| 事件 | 触发时机 | 记录内容 |
|-----|---------|---------|
| view | 打开详情页 | skill_id, source |
| install | 复制安装命令 | skill_id, source |
| search | 执行搜索 | query, results_count |

### 6.2 实现

```typescript
// 详情页加载时记录 view
onMount(() => {
  fetch('/api/events', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      skill_id: skill.id,
      event_type: 'view',
      source: 'web'
    })
  });
});

// 搜索时记录
afterSearch((results) => {
  fetch('/api/search-log', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      query: searchQuery,
      results_count: results.length,
      top_results: results.slice(0, 5).map(r => r.id)
    })
  });
});
```

## 七、响应式设计

### 断点

| 断点 | 宽度 | 用途 |
|-----|-----|-----|
| sm | 640px | 手机横屏 |
| md | 768px | 平板 |
| lg | 1024px | 小笔记本 |
| xl | 1280px | 桌面 |

### 关键适配

- **首页分类**：xl:4列 → lg:3列 → sm:2列
- **搜索结果**：单列列表，充分利用宽度
- **详情页**：内容区域最大宽度 4xl，居中显示
- **卡片**：关键信息在小屏幕不截断
